interface:
  display_name: "Caruso Recipe Book"
  short_description: "Add a family recipe from Codex or Claude"
  default_prompt: "Use the Caruso Recipe Book skill to interview me, preview one recipe, and publish it through the add-only service."

policy:
  allow_implicit_invocation: true
