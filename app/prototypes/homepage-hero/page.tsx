import PrototypeHomepageHero from "./prototype-homepage-hero";

export const metadata = {
  title: "Principles Section Animation Prototypes | DaytonGrowthCo.",
  robots: { index: false, follow: false },
};

export default function HomepageHeroPrototypePage() {
  return <PrototypeHomepageHero />;
}
